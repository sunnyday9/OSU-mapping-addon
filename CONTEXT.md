# CONTEXT.md — 领域词汇表

osu! AI Studio 的共享领域语言。术语在此定义后，代码、issue、ADR、提交信息都应使用同一名称。

## 词汇

### Mapping IR 管线（新链）

统一的谱面生成决策链：音频分析 → 音乐时间线 → 证据 → 全局/本地规划 → 候选生成与排名 → 渲染 → 校验 → Critic → SR 校准闭环。内存产物是 MappingDocument，可渲染为完整 .osu。Mania 模式的唯一编辑器生成路径（ADR-011）。

### M2 生成链（旧链）

早期"分析 → 规则/模板合成"直出谱面的链路。osu! 模式仍在使用；Mania 模式已由 Mapping IR 管线取代并删除（ADR-011）。

### 密度旋钮（DensityScale）

SR 校准期间调节谱面密度的标量乘子。不同生成路径有各自的取值范围（领域旋钮），但调节算法全局唯一（见收敛核）。

### SR 校准闭环

反复以不同密度旋钮值重跑生成，直到实测 SR 落入目标 ± 容差；有界迭代 + 停滞保护。

### 收敛核

SR 校准闭环唯一的数值收敛实现。所有校准路径（文档环、各模式生成器）必须经由它；禁止在任何地方内联收敛公式（架构走查候选 5 的教训）。

### 实测 SR

官方难度计算器对最终渲染产物计算出的星数。不变量：**报告 SR = 实测 SR**——任何向用户展示的星数必须来自实测 SR，禁止两套算法并存。

### 目标难度档案（DifficultyProfile）

目标星数、容差、维度偏好（节奏/密度/移动/技术性等）与模式偏好的集合；生成与校准的输入。

### 质量门禁（Quality Gates）

生成产物落盘前必须通过的校验集合（validator / critic / QualityGateRunner）。约定：门禁不通过不落盘（`Success=false` 时不产生文件）。
